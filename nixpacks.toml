# Nixpacks Configuration for Railway
# Deploy BACKEND ONLY - Frontend is on Vercel

[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["cd backend && npm ci"]

[phases.build]
cmds = ["echo 'No build needed for backend'"]

[start]
cmd = "cd backend && node server.js"
