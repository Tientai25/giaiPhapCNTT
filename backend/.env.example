# Environment variables for backend (PostgreSQL)
# Copy this file to .env and update with your values

# PostgreSQL Database Configuration
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=tdt_econtract

# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Secret (generate a strong random string)
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# CORS - Allowed Origins (Frontend URLs)
CORS_ORIGIN=http://localhost:5173

# For Production (Render/Railway deployment):
# PGHOST=dpg-xxxxx.singapore-postgres.render.com
# PGPORT=5432
# PGDATABASE=tdt_econtract
# PGUSER=tdt_user
# PGPASSWORD=your_render_password
# JWT_SECRET=your_production_secret
# CORS_ORIGIN=https://giai-phap-cntt.vercel.app
